Put your resume files here, matching the `resume_path` values in `config.yaml`:

- `sales_resume.pdf`
- `hr_resume.pdf`

This directory's file contents are gitignored (only this README is tracked)
since resumes carry personal contact details that shouldn't sit in git
history.
